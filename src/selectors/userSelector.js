export const selectUsername = (state) => state.user.username;
export const selectSessionId = (state) => state.user.sessionId;
export const selectAccountId = (state) => state.user.accountId;
export const selectFavoriteMovies = (state) => state.user.favoriteList;
export const selectWatchListMovies = (state) => state.user.watchList;
export const selectFavoriteListStatus = (state) => state.user.favoriteStatus;
export const selectwatchListStatus = (state) => state.user.watchListStatus;
